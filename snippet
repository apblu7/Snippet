MethodSignature signature = (MethodSignature) joinPoint.getSignature();
Method method = signature.getMethod();

// 실제 클래스가 아닌 인터페이스 메소드에서 애노테이션 확인
Method interfaceMethod = joinPoint.getTarget()
        .getClass()
        .getInterfaces()[0]  // 주의: 인터페이스가 여러 개일 경우 주의 필요
        .getMethod(method.getName(), method.getParameterTypes());

if (interfaceMethod.isAnnotationPresent(NoLogging.class)) {
    return joinPoint.proceed();
}