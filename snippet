<build>
    <plugins>
        <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>1.8</version>
            <executions>
                <execution>
                    <id>run-deploy-bat</id>
                    <phase>install</phase> <!-- 또는 package -->
                    <configuration>
                        <target>
                            <exec executable="cmd">
                                <arg value="/c"/>
                                <arg value="${basedir}\deploy\deploy.bat"/>
                            </exec>
                        </target>
                    </configuration>
                    <goals>
                        <goal>run</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>
    </plugins>
</build>