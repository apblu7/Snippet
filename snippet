<insert id="insertExample" parameterType="list">
    INSERT INTO example_table (id, name, value) 
    VALUES 
    <foreach item="item" index="index" collection="list" separator=",">
        (#{item.id}, #{item.name}, #{item.value})
    </foreach>
</insert>